<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build-only network security config.

  BASE_URL for debug builds is hardcoded to http://10.0.2.2:4000 (the
  emulator's alias for the host machine's localhost — see app/build.gradle.kts).
  Android 9+ (API 28+) blocks cleartext (plain HTTP) traffic by default, so
  without this override every request from a debug build fails immediately
  with UnknownServiceException: CLEARTEXT communication to 10.0.2.2 not
  permitted by network security policy — before it ever reaches the server.

  This file lives ONLY in src/debug, so it is never merged into release
  builds (which use https://api.filipinodama.com and get the platform
  default of cleartext-disabled).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
