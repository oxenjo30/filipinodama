{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "corepack enable && corepack prepare pnpm@9.15.9 --activate && pnpm install --frozen-lockfile=false && pnpm --filter @dama/shared build && pnpm --filter @dama/game-engine build && pnpm --filter server exec prisma generate && pnpm --filter server build"
  },
  "deploy": {
    "preDeployCommand": "corepack enable && corepack prepare pnpm@9.15.9 --activate && pnpm --filter server exec prisma migrate deploy && pnpm --filter server exec prisma db seed",
    "startCommand": "node apps/server/dist/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
