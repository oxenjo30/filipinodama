node_modules/
dist/
dist-ssr/
.next/
.turbo/
coverage/
.env
.env.*
!.env.example
!.env.test.example
*.log
.DS_Store
apps/server/prisma/migrations/dev.db*
apps/web/dev-dist/

# Large design source bundle — never commit (98MB); design truth lives in handoff/
*.zip
FilipinoDama Web Game Design.zip

# TS build info
*.tsbuildinfo

# Design-review reference material — not needed to build/run the app.
# Shipped cosmetics live in apps/web/public/assets/. Kept on disk, out of the repo.
handoffv2/
handoff/
_asset-previews/

# Local AAB/APK delivery folder (build artifacts — never commit)
/builds/

# Owner's local asset libraries — precious, on-disk-only, never committed.
# MUST stay gitignored: ignored files are immune to `git stash -u` and plain
# `git clean -fd` (a blanket `git stash push -u` swept these into a stash on
# 2026-07-19; recovered — see tasks/lessons.md). Never run `git clean -x` here.
/play-store-assets/
/handoffv3/
/.backups/
